version: "2.1"
services:
  deemix:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  sonarr:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  bazarr:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  heimdall:
    tmpfs:
      - /tmp
      - /var/tmp
      - /var/log/php7
      - /var/log/nginx
      - /var/spool
      - /var/cache
  lidarr:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  jackett:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  jellyfin:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  radarr:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  syncthing:
    tmpfs:
      - /tmp
      - /var/log
      - /var/tmp
      - /var/spool
      - /var/cache
  transmission:
    tmpfs:
      - /tmp
      - /var/cache
      - /var/log
      - /var/spool
      - /var/tmp
